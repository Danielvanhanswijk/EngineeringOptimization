function [xr, yr, zr, psir] = trajectorygen( N_steps, t)

%     xr = sin(t) ;
%     yr = t.*cos(t) ;
%     zr = -sin(t) ;
%     psir = zeros(1,N_steps);
    
    xr = zeros(1,N_steps);
    yr = zeros(1,N_steps);
    zr =5*ones(1,N_steps);
    psir = zeros(1,N_steps);
     

end